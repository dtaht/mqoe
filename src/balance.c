// BinPacking and the knapsack problem are old ones in computer science

int weight(int algo, nodes *p) {
	switch(algo) {
	case 0: break; // no balancing 
	case 1: break; // The original python algo
	case 2: break; // One that makes assumptions about the total
	}
}

balance(nodes *p) {
}

// in all cases we reverse the balance so the weakest node is on 0


