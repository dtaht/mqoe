// A bare string without a specifier converts from that
// to kbit
u64 convert_kbits(char *val, char *assume) {
}

// A bare string without a specifier converts from that
u64 convert_ns(char *val, char *assume) {

}
